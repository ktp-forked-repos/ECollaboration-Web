package com.general.param.test;

import java.util.ArrayList;
/*

import com.general.param.tutor.ScoreForTeamToSub;
import com.general.param.tutor.ScoreForTeamToSub.ScoreService;
import com.general.param.tutor.ScoreForTeamToSub.ScoreService.ScoreDetail;
*/

//也可以不导入包，则类要写成全类名，如ScoreDetail->ScoreForTeamToSub.ScoreService.ScoreDetail
public class ScoreForTeamToSubTest {
	
	public static void main(String args[]){
/*

		ScoreForTeamToSub sfts = new ScoreForTeamToSub();
		ScoreService score = sfts.newScore();
		ArrayList<ScoreDetail> scores = score.newScores();
		ScoreDetail scoreDetail = score.newScore();
		scores.add(scoreDetail);

*/
	}

}
